public with sharing class CalculatorClass {
    public Integer addThreeNumberChangesforMerge(Integer a, Integer b, Integer c) {
        return a + b + c;
        system.debug('Sum calculated successfully.');
}