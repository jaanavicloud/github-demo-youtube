public with sharing class sumOfTwoNumbersClass {
    public sumOfTwoNumbers(Integer One, Integer Two) {
        Integer sum = One + Two;
        System.debug('The sum of ' + One + ' and ' + Two + ' is: ' + sum);
    }
}